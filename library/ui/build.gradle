apply plugin: 'com.android.library'
apply from: '../../base.gradle'

android {
    defaultConfig {
        versionCode 1
        versionName "1.0"
    }
    //将res文件夹中的资源以模块的形式分类
    sourceSets {
        main {
            res.srcDirs = [
                    'src/main/res/circle',
                    'src/main/res/dialog',
                    'src/main/res/drag',
                    'src/main/res/progress',
                    'src/main/res/tag',
                    'src/main/res/toast',
                    'src/main/res'
            ]
        }
    }
}
